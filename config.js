// 預測後端（Hugging Face Space）網址。部署 Space 後把這裡換成你的 Space URL。
// UI 內也可即時修改（會存進瀏覽器 localStorage，優先於此預設值）。
window.TEXTILE_CONFIG = {
  SPACE_URL: "https://YOUR-HF-USERNAME-textile-yolo-predict.hf.space"
};
